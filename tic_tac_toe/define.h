#pragma once
#ifndef HEADER_H
#define HEADER_H

#define CAPACITY_FULL 9

typedef enum { TRUE, FALSE } BOOL;

typedef enum { CONTINUE, END } EXIT;

typedef enum { UNKNOWN, DROW, O_WIN, X_WIN } WIN;

#endif