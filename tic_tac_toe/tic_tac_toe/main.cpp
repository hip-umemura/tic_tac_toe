#include <stdio.h>

int main(void) {
  int is_tutorial;
  char name1;
  char name2;

  printf("チュートリアルを見ますか(YES...1 / NO...1以外)：");
  scanf_s("%d", &is_tutorial);

  if (is_tutorial == 1) {

  }

  return 0;
}