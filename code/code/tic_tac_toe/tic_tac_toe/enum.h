#pragma once

typedef enum
{
    ERROR,
    RETRY,
    FINISH,
    PLAYER1_WIN,
    PLAYER2_WIN,
    DRAW,
    CONTINUE
} JUDGE;

typedef enum
{
    PLAYER1,
    PLAYER2
}PLAYER;

#define INDEX (3)
