typedef enum {
	TRUE,
	FALSE,
}BOOL;

typedef enum {
	CIRCLE,
	CROSS
}MARK;

typedef enum {
	CIRCLE_WIN,
	CROSS_WIN,
	DROW
}JUDGE;

typedef enum {
	TRY,
	QUIT,
	ERROR
}RETRY;

typedef enum {
	END,
	EXTEND
};

